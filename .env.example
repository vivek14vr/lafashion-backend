DATABASE_URL=mongodb://127.0.0.1:27017/event-sharon
PAYLOAD_SECRET=replace-with-a-long-random-secret
# Public URL where the site (and proxied /admin) is served
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
# Direct Payload Next app (rewrites target this)
PAYLOAD_BACKEND_URL=http://localhost:3001

# Cloudinary (free tier) — optional for local; required in production
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_FOLDER=la-fashion-closet
